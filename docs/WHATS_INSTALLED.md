# What's Installed

A one-page reference of every file, plugin, and tool the kit touches. Useful when you're trying to understand what something is, or troubleshooting *"where did this come from?"*

---

## On your computer

| File / app | Where it lives | What it does | Source |
|---|---|---|---|
| Claude Code | App, anywhere | The AI assistant — the kit plugs into it | [claude.ai/download](https://claude.ai/download) |
| Local by Flywheel *(optional)* | App + `~/Local Sites/` | Free local WordPress hosting | [localwp.com](https://localwp.com/) |
| Git Bash *(Windows only)* | `C:\Program Files\Git\` | Provides `bash`, `curl`, `python`, `zip` | [git-scm.com](https://git-scm.com/download/win) |
| `SKILL.md` | `~/.claude/skills/elementor-mcp/` | Cheat sheet Claude reads to know how to use the MCP correctly | This kit |
| `setup-elementor-mcp.sh` | `~/.claude/scripts/` | Wizard that wires Claude to a WordPress site | This kit |
| `.mcp.json` | *(your project folder, after running the wizard)* | Per-project MCP server config — tells Claude Code which WordPress site to connect to | Generated by wizard |

---

## On your WordPress site

### Required

| Plugin / Theme | What it does | Installed by |
|---|---|---|
| **Elementor (free)** | The page builder. Claude builds pages inside this. | You manually OR wizard step 6 |
| **Hello Elementor** *(theme)* | Blank canvas theme. Other themes inject CSS that fights Elementor. | You manually *(theme switch isn't via REST in all WP versions)* |
| **Ultimate Addons for Elementor (UAE)** | Builds site-wide headers/footers (Theme Builder is Pro-only). Includes a free Nav Menu widget. | You manually OR wizard step 6 |
| **MCP Adapter** | The "phone line" that lets any AI server talk to WordPress. | Wizard step 7 *(automatic — from GitHub)* |
| **MCP Tools for Elementor** | The Elementor-specific MCP server. Exposes ~75 Elementor tools to Claude. | Wizard step 7 *(automatic — from GitHub)* |

### Optional but useful

| Plugin | What it does | Installed by |
|---|---|---|
| **Essential Addons (lite)** | Free widgets like Post Grid that aren't in Elementor base | Wizard step 6 *(if you opt in)* |
| **Fluent Forms** | Real working contact forms (Elementor's Form widget is Pro) | Wizard step 6 *(if you opt in)* |

### What gets created on the site itself

When you tell Claude to build, the following data is written to the WordPress database:

| Type | Where | Notes |
|---|---|---|
| Pages (`page` post type) | Standard WordPress pages | The Home page, About, Services, etc. |
| Header/footer templates (`elementor-hf` post type) | UAE templates | One per template — site-wide headers, footers |
| `_elementor_data` post meta | Each page's row in `wp_postmeta` | Big JSON blob describing every container/widget |
| Global Site Settings | `_elementor_active_kit` option | Brand colors, typography |

Nothing destructive happens to existing content unless you explicitly ask Claude to delete or restructure.

---

## Where to find things in WP Admin

| Looking for | Path |
|---|---|
| Pages (the homepage etc.) | **Pages** |
| Headers + footers | **Appearance → Header Footer Builder** |
| WordPress menus *(for navigation links)* | **Appearance → Menus** |
| Brand colors / fonts | **Elementor → Site Settings** *(or click "≡ → Site Settings" inside the Elementor editor)* |
| Active plugins | **Plugins → Installed Plugins** |
| Active theme | **Appearance → Themes** |
| Application Passwords | **Users → Profile → scroll down** |
| Fluent Forms submissions | **Fluent Forms → Entries** |

---

## Where to find things in this kit

| Looking for | Path |
|---|---|
| The cheat sheet Claude reads | `files/SKILL.md` *(installed to `~/.claude/skills/elementor-mcp/`)* |
| The setup wizard | `files/setup-elementor-mcp.sh` *(installed to `~/.claude/scripts/`)* |
| Mac/Linux installer | `INSTALL.sh` |
| Windows installer | `INSTALL.bat` *(double-click)* or `INSTALL.ps1` |
| Quick-start guide | `docs/QUICKSTART.md` |
| Deeper "why this works the way it does" | `docs/LESSONS.md` |
| This page | `docs/WHATS_INSTALLED.md` |
| License | `LICENSE` *(MIT — free for any use)* |

---

## How the kit installs things

1. **`INSTALL.sh` / `INSTALL.bat`** copies `SKILL.md` and `setup-elementor-mcp.sh` into `~/.claude/`. That's it for the kit's local install.

2. **`setup-elementor-mcp.sh` (the wizard)** does the WordPress side:
   - **Step 6** *(optional, asks first)* — REST-installs Elementor + UAE + Hello Elementor theme + optional Essential Addons & Fluent Forms from wordpress.org
   - **Step 7** *(automatic)* — downloads MCP Adapter and MCP Tools for Elementor from GitHub releases, repacks them with clean folder names, and installs via WP-CLI (Local sites) or asks you to upload via WP Admin (live sites)

3. **`.mcp.json`** is written to your current directory — this is what Claude Code reads to know which WordPress site to connect to.

---

## How to uninstall

**Remove the kit from your computer:**
```bash
rm -rf ~/.claude/skills/elementor-mcp
rm ~/.claude/scripts/setup-elementor-mcp.sh
```

**Remove from a WordPress site:**

WP Admin → Plugins → Deactivate + Delete the plugins you want gone. The MCP plugins (`mcp-adapter` and `elementor-mcp`) are safe to remove without affecting your Elementor pages — they only enable AI-driven editing, not the editor itself.

**Remove the project's `.mcp.json`:**
```bash
rm .mcp.json
```

---

## Credits

- **[elementor-mcp](https://github.com/msrbuilds/elementor-mcp)** by [@msrbuilds](https://github.com/msrbuilds) — GPL-3.0
- **[WordPress MCP Adapter](https://github.com/WordPress/mcp-adapter)** — GPL-2.0
- **Elementor**, **Hello Elementor**, **UAE**, **Essential Addons**, **Fluent Forms** — third-party plugins, each with their own licenses (visit their wordpress.org pages for details)

This kit's own files (skill, setup script, installers, docs) are MIT-licensed.
